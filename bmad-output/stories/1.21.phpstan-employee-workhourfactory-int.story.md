# Story 1.21 — PHPStan: Employee WorkHourFactory Int Cast Fix

## Story

**As a** developer  
**I want** to fix mixed-to-int cast errors in WorkHourFactory  
**So that** faker method returns are properly typed  

---

## Acceptance Criteria

- [ ] AC1: `workDaySequence()` method has no `(int) cast` on faker results
- [ ] AC2: PHPStan level max reports 0 errors on WorkHourFactory

---

## Tasks / Subtasks

- [ ] T1: Replace `(int) $this->faker->randomElement([...])` with proper typing (AC: 1)
- [ ] T2: Add `@var int` docblock for faker results (AC: 1)
- [ ] T3: Run PHPStan on WorkHourFactory (AC: 2)

---

## Dev Notes

**Source:** PHPStan error `cast.int` on line 72.

**File:** `Modules/Employee/database/factories/WorkHourFactory.php`

**Fix pattern:**
```php
$minutes = [0, 15, 30, 45];
/** @var int $clockInMinute */
$clockInMinute = $this->faker->randomElement($minutes);
```

Faker's `randomElement()` returns `T` from array, so the `@var int` annotation tells PHPStan the type.

---

## Testing Strategy

- Run `./vendor/bin/phpstan analyse Modules/Employee/database/factories/WorkHourFactory.php`

---

## Owned File/Module Scope

- `Modules/Employee/database/factories/WorkHourFactory.php`

---

## Status

`backlog`

---

## Dev Agent Record

_Empty_
