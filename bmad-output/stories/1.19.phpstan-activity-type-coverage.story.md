# Story 1.19 — PHPStan: Activity RedactModelAttributesAction Type Coverage Fix

## Story

**As a** developer  
**I want** to fix the PHPStan type coverage error in `RedactModelAttributesAction.php`  
**So that** constant types are properly typed for 99%+ coverage  

---

## Acceptance Criteria

- [ ] AC1: `SENSITIVE_KEYS` constant has explicit type annotation
- [ ] AC2: PHPStan level max reports 0 errors on Activity module
- [ ] AC3: All other constant types in Activity module are explicitly typed

---

## Tasks / Subtasks

- [ ] T1: Add `@var list<string>` or explicit type to `SENSITIVE_KEYS` constant (AC: 1)
- [ ] T2: Audit all constants in Activity module for type coverage (AC: 3)
- [ ] T3: Run PHPStan on Activity module and verify 0 errors (AC: 2)

---

## Dev Notes

**Source:** PHPStan error `typeCoverage.constantTypeCoverage` - 90.5% coverage.

**File:** `Modules/Activity/app/Actions/RedactModelAttributesAction.php`

The constant already has `@var list<string>` but PHPStan needs the type annotation directly on the const:

```php
/** @var list<string> */
private const SENSITIVE_KEYS = [...];
```

Or use PHP 8.3+ typed constants if the PHP version supports it.

---

## Testing Strategy

- Run `./vendor/bin/phpstan analyse Modules/Activity` and verify 0 errors

---

## Owned File/Module Scope

- `Modules/Activity/app/Actions/RedactModelAttributesAction.php`

---

## Status

`backlog`

---

## Dev Agent Record

_Empty_
