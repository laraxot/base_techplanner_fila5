---
name: 4.1.parallel-phpstan-fix
title: "Fix all PHPStan errors across all modules in parallel via swarm + sub-agents"
status: backlog
module: All
---

# 4.1: Parallel PHPStan Fix (Swarm + Sub-Agents)

**Story ID:** 4.1
**Epic:** 4 — PHPStan Zero-Error (Parallel)
**Slug:** parallel-phpstan-fix
**Status:** backlog → ready-for-dev

## Story
As a **maintainer**, I want all PHPStan errors fixed across all modules in parallel using swarm + sub-agents, following the correct git workflow per module.

## Acceptance Criteria
1. BMAD stories created for each module fix (one story per module)
2. Each file edit validated with PHPStan + PHPMD + PHPInsights + Pest before commit
3. Git workflow per module: `git add -A` → `git commit` → `git fetch laraxot dev` → `git merge laraxot/dev --allow-unrelated-histories -s resolve` → `git push -u` (NO --force)
4. No `phpstan.neon` modification (only user can modify)
5. `mixed` types replaced with specific types where found
6. Docs updated in modules/themes
7. No `--force` push on any module

## Tasks (parallel, one sub-agent per module)
- [ ] Xot: Fix merge conflict in Helper.php + generics (3 errors)
- [ ] User: Fix generics (Notification, Permission, PersonalAccessToken, Role) + BaseUserFactory issue (6 errors)
- [ ] Media: Fix undefined $actions in tests (17 errors)
- [ ] Activity: Verify 0 errors after generics fix
- [ ] Geo: Verify 0 errors after generics fix
- [ ] Job: Verify 0 errors after generics fix
- [ ] Lang: Verify 0 errors after generics fix
- [ ] Tenant: Verify 0 errors after generics fix
- [ ] Notify, UI, TechPlanner, Employee, AI, Cms, Seo, Gdpr: Verify 0 errors

## Dev Notes
- Module repos are independent git repos (NOT submodules), each has .git
- gitmodules.ini is just a tracker, not git submodules
- Git flow: add -A → commit → fetch laraxot/dev → merge laraxot/dev --allow-unrelated-histories -s resolve → push -u (no --force)
- Fix order: resolve conflicts first, then generics, then undefined vars
- Source: PHPStan L10 report (31 errors total across User, Xot, Media, others)
- Constraint: NEVER touch phpstan.neon; NEVER add @phpstan-ignore [Source: second brain]

## Testing
- PHPStan L10: 0 errors per module
- PHPMD: <10 cyclomatic per method
- PHPInsights: quality score maintained
- Pest: tests pass on affected module

## Dependency Maps
- Blocked by: 3.1 (generics analysis completed)
- Blocks: 1.1-1.17 (module philosophy docs)

## Owned File/Module Scope
- `Modules/*/app/Models/*.php` (generics fixes)
- `Modules/*/tests/Unit/Filament/*.php` (undefined vars)
- `Modules/Xot/helpers/Helper.php` (merge conflict)

## Learnings
(from 3.1-3.2 — generics fix pattern established)

## Dev Agent Record
_(to be completed per module after fix)_
