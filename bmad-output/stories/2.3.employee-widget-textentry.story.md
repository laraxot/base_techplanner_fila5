# 2.3: Fix Employee Widget TextEntry Issues

**Story ID:** 2.3
**Epic:** 2 — Employee Module Fixes
**Slug:** employee-widget-textentry
**Status:** ready-for-dev

## Story
As a developer,
I want to fix `TextEntry` unknown class errors in 3 Employee widgets,
so that PHPStan reports zero unknown class errors.

## Acceptance Criteria
1. `TextEntry` references corrected using Filament v5 schema API (`Text::make()` from `Filament\Schemas`)
2. `.content()` calls removed/replaced
3. All 3 widgets compile and PHPStan passes

## Tasks / Subtasks
- [ ] Fix `AttendanceOverviewWidget.php` (AC: 1)
- [ ] Fix `LeaveBalanceWidget.php` (AC: 1)
- [ ] Fix `TeamPresenceWidget.php` (AC: 1)
- [ ] Verify `php -l` and PHPStan (AC: 2, 3)

## Dev Notes
- Use `Filament\Schemas\Components\Text::make()` not `Filament\Infolists\Components\TextEntry` [Source: tech-spec.md#FR-004]
- Remove `.content()` calls; use `Placeholder::make()` or `Text::make()` appropriately [Source: previous fix in `LeaveBalanceWidget.php`]

## Owned File/Module Scope
- `Employee/app/Filament/Widgets/AttendanceOverviewWidget.php`
- `Employee/app/Filament/Widgets/LeaveBalanceWidget.php`
- `Employee/app/Filament/Widgets/TeamPresenceWidget.php`
