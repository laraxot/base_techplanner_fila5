# Story 1.25 — PHPStan: Employee Model Constant Type Fixes

## Story

**As a** developer  
**I want** to add explicit types to all model constants  
**So that** PHPStan achieves 99%+ type coverage on Employee models  

---

## Acceptance Criteria

- [ ] AC1: TimeEntry constants have `string` type annotation
- [ ] AC2: AbsenceRequest constants have `string` type annotation
- [ ] AC3: WorkHour constants have proper `@var list<string>` annotation
- [ ] AC4: PHPStan reports 0 type coverage errors

---

## Tasks / Subtasks

- [ ] T1: Add `string` type to TimeEntry status constants (AC: 1)
- [ ] T2: Add `string` type to AbsenceRequest constants (AC: 2)
- [ ] T3: Add `@var list<string>` to WorkHour TYPES and STATUSES (AC: 3)
- [ ] T4: Run PHPStan on all Employee models (AC: 4)

---

## Dev Notes

**Source:** PHPStan `typeCoverage.constantTypeCoverage` errors (90.5%).

**Files:**
- `Modules/Employee/app/Models/TimeEntry.php`
- `Modules/Employee/app/Models/AbsenceRequest.php`
- `Modules/Employee/app/Models/WorkHour.php`

**Fix pattern (PHP 8.3+ typed constants):**
```php
public const string STATUS_PENDING = 'pending';
public const string STATUS_APPROVED = 'approved';
```

Or for arrays:
```php
/** @var list<string> */
public const TYPES = [...];
```

---

## Testing Strategy

- Run `./vendor/bin/phpstan analyse Modules/Employee/app/Models/`

---

## Owned File/Module Scope

- `Modules/Employee/app/Models/TimeEntry.php`
- `Modules/Employee/app/Models/AbsenceRequest.php`
- `Modules/Employee/app/Models/WorkHour.php`

---

## Status

`backlog`

---

## Dev Agent Record

_Empty_
