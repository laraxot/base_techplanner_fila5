# Story 1.23 — PHPStan: Employee CreateWorkHour Timestamp Type Fix

## Story

**As a** developer  
**I want** to fix mixed-to-string/int cast errors in CreateWorkHour  
**So that** form data is properly validated before use  

---

## Acceptance Criteria

- [ ] AC1: `beforeCreate()` uses `is_string()` checks for timestamp
- [ ] AC2: `employee_id` uses `is_numeric()` check before int cast
- [ ] AC3: PHPStan level max reports 0 errors on CreateWorkHour

---

## Tasks / Subtasks

- [ ] T1: Replace `strval($data['timestamp'] ?? '')` with typed check (AC: 1)
- [ ] T2: Replace `intval($data['employee_id'] ?? 0)` with typed check (AC: 2)
- [ ] T3: Add `@var string` and `@var int` annotations (AC: 1, 2)
- [ ] T4: Run PHPStan on CreateWorkHour (AC: 3)

---

## Dev Notes

**Source:** PHPStan errors `argument.type` for strval/intval.

**File:** `Modules/Employee/app/Filament/Resources/WorkHourResource/Pages/CreateWorkHour.php`

**Fix pattern:**
```php
/** @var string $ts */
$ts = is_string($data['timestamp'] ?? null) ? $data['timestamp'] : '';
$timestamp = Carbon::parse($ts);

$employeeId = 0;
if (is_numeric($data['employee_id'] ?? null)) {
    /** @var int $employeeId */
    $employeeId = (int) $data['employee_id'];
}
```

---

## Testing Strategy

- Run `./vendor/bin/phpstan analyse Modules/Employee/app/Filament/Resources/WorkHourResource/Pages/CreateWorkHour.php`

---

## Owned File/Module Scope

- `Modules/Employee/app/Filament/Resources/WorkHourResource/Pages/CreateWorkHour.php`

---

## Status

`backlog`

---

## Dev Agent Record

_Empty_
