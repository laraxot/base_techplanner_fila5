# 1.7: PHPStan L10 Media Test Files Fix

**Story ID:** 1.7
**Epic:** 1 — Prompt Directory Analysis & Cleanup (ext.)
**Slug:** phpstan-media-tests-fix
**Status:** ready-for-dev

## Story

As a **PHPStan L10 maintainer**,
I want **Media test files to have correct variable scope and array typing**,
so that **PHPStan reports zero `variable.undefined` and `argument.type` errors in Media tests**.

## Acceptance Criteria

1. Zero `variable.undefined` errors in `Media/tests/Unit/Filament/MediaConvertSchemasTest.php`.
2. Zero `variable.undefined` errors in `Media/tests/Unit/Filament/MediaTableTest.php`.
3. Zero `argument.type` errors for `assertArrayHasKey` calls.
4. PHPStan L10: 0 errors in Media module.

## Tasks / Subtasks

- [ ] Read each Media test file (AC: #1, #2)
- [ ] Identify scope of `$actions` variable (where defined, where used) (AC: #1, #2, #3)
- [ ] Add type assertion or restructure test setup (AC: #1, #2, #3)
- [ ] Run PHPStan L10 on Media (AC: #4)

## Dev Notes

- `assertArrayHasKey` signature: `assertArrayHasKey(string|int $key, array<mixed>|ArrayAccess $array)` [Source: phpstan]
- `$actions` is likely the result of a Filament Table builder call, which returns `array<mixed>` by default
- Pattern: `Assert::isArray($actions);` then pass to assertArrayHasKey
- OR: type the variable explicitly with `/** @var array<string, mixed> $actions */`

## Testing

- **Approach:** PHPStan L10
- **Per-AC verification:** `phpstan analyse Modules/Media` reports 0 errors

## Dependency Maps

- **Blocked by:** none
- **Blocks:** none

## Owned File/Module Scope

- `laravel/Modules/Media/tests/Unit/Filament/MediaConvertSchemasTest.php`
- `laravel/Modules/Media/tests/Unit/Filament/MediaTableTest.php`

## Dev Agent Record

_(empty — to be completed by the external dev tool)_
